## TODO Regexes

# FDSWAVE [inst] : [data]
^\s*(?P<tag>\w+)\s+(?P<inst_index>\d+)\s*\:\s*(?P<data>.*)$

# FDSMOD [inst] : [data]
^\s*(?P<tag>\w+)\s+(?P<inst_index>\d+)\s*\:\s*(?P<data>.*)$

# FDSMACRO [inst] [type] [loop] [release] [setting] : [macro]
^\s*
(?P<tag>\w+)       # tag
\s+
(?P<inst>\d+)       # inst
\s+
(?P<type>\d+)       # type
\s+
(?P<loop>\-?\d+)    # loop
\s+
(?P<release>\-?\d+)    # release
\s+
(?P<setting>\d+)       # setting
\s*\:\s*
(?P<list>.*)$       # list

re.findall(r'\-?\d+', x.group('list'))

# N163WAVE [inst] [wave] : [data]
^\s*(\w+)\s+(\d+)\s+(\d+)\s*\:\s*(.*)$

# TRACK [pattern] [speed] [tempo] [name]
^\s*(?P<tag>TRACK)\s+(?P<num_rows>\d+)\s+(?P<speed>\d+)\s+(?P<tempo>\d+)\s*\"(?P<name>.*)\".*$

# COLUMNS : [columns]
^\s*(?P<tag>COLUMNS)\s*\:\s*(?P<data>.*)$
efF_cols = list(map(int, re.findall(r'\d+', x.group('data'))))
num_cols = len(eff_cols)

# PATTERN [pattern]
^\s*(?P<tag>PATTERN)\s+(?P<pattern>[0-9a-fA-F]{2}).*$

# ROW [hex_str] : [data]
^\s*(?P<tag>ROW)\s+(?P<row_hex>[0-9a-fA-F]{2})\s*\:\s*(?P<data>.*)$

