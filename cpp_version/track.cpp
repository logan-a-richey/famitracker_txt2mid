// track.cpp

#include <string>
#include <track.h>

// constructor
Track::Track(
    int _num_rows,
    int _speed,
    int _tempo
    std::string _name,
){
    num_rows = _num_rows,
    speed = _speed,
    tempo = _tempo
    name = _name,
}


